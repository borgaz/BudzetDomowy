<Page x:Class="Budget.WelcomePage.WelcomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      Loaded="Page_Loaded"
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="725"
	Title="WelcomePage">

    <Grid>
        <Image Source="../Resources/Pie-Chart-icon.png" HorizontalAlignment="Left" Height="235" Margin="452,62,0,0" VerticalAlignment="Top" Width="260"/>
        <DataGrid HorizontalAlignment="Left" Name="shortHistoryDataGrid" ItemsSource="{Binding}" AutoGenerateColumns="False" Margin="10,306,0,0" VerticalAlignment="Top" Height="234" Width="375" Loaded="Page_Loaded">
            <DataGrid.Columns>
                <DataGridTextColumn Header="+/-" Width="40" IsReadOnly="True" Binding="{Binding Sign}"/>
                <DataGridTextColumn Header="Typ" Width="70" IsReadOnly="True" Binding="{Binding Type}"/>
                <DataGridTextColumn Header="Nazwa" Width="100" IsReadOnly="True" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Kwota" Width="60" IsReadOnly="True" Binding="{Binding Amount}"/>
                <DataGridTextColumn Header="Data" Width="80" IsReadOnly="True" Binding="{Binding Date}"/>
            </DataGrid.Columns>
        </DataGrid>
        <DataGrid HorizontalAlignment="Left" Name="providedPaymentsDataGrid" ItemsSource="{Binding}" AutoGenerateColumns="False" CanUserResizeColumns="False" Margin="10,38,0,0" VerticalAlignment="Top" Height="234" Width="375" Loaded="Page_Loaded" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="+/-" Width="40" IsReadOnly="True" Binding="{Binding Sign}"/>
                <DataGridTextColumn Header="Typ" Width="70" IsReadOnly="True" Binding="{Binding Type}"/>
                <DataGridTextColumn Header="Nazwa" Width="100" IsReadOnly="True" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Kwota" Width="60" IsReadOnly="True" Binding="{Binding Amount}"/>
                <DataGridTextColumn Header="Data" Width="80" IsReadOnly="True" Binding="{Binding Date}"/>
            </DataGrid.Columns>
        </DataGrid>
        <DataGrid HorizontalAlignment="Left" Margin="440,366,0,0" VerticalAlignment="Top" Height="160" Width="260"/>
        <Label Content="Najbliższe przychody i wydatki:" HorizontalAlignment="Left" Height="28" Margin="10,10,0,0" VerticalAlignment="Top" Width="191"/>
        <Label Content="Cele oszczędzania:" HorizontalAlignment="Left" Height="28" Margin="441,328,0,0" VerticalAlignment="Top" Width="199"/>
        <Label Content="Skrócona historia:" HorizontalAlignment="Left" Height="28" Margin="10,278,0,0" VerticalAlignment="Top" Width="164"/>
        <Label Content="Aktualne saldo:" HorizontalAlignment="Left" Height="25" Margin="551,13,0,0" VerticalAlignment="Top" Width="94"/>
        <TextBox Name="Balance" HorizontalAlignment="Left" Height="22" Margin="649,16,0,0" TextWrapping="Wrap" IsReadOnly="True" Text="TextBox" VerticalAlignment="Top" Width="61"/>
    </Grid>
</Page>
