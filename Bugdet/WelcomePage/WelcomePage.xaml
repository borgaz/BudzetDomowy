<Page x:Class="Budget.WelcomePage.WelcomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      Loaded="Page_Loaded"
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="725"
	Title="WelcomePage">

    <Grid>
        <DataGrid Name="shortHistoryDataGrid" ItemsSource="{Binding}" AutoGenerateColumns="False" Margin="10,0,0,10" HorizontalAlignment="Left" VerticalAlignment="Bottom" Height="234" Width="375" Loaded="Page_Loaded">
            <DataGrid.Columns>
                <DataGridTextColumn Header="+/-" Width="40" IsReadOnly="True" Binding="{Binding Sign}"/>
                <DataGridTextColumn Header="Typ" Width="70" IsReadOnly="True" Binding="{Binding Type}"/>
                <DataGridTextColumn Header="Nazwa" Width="105" IsReadOnly="True" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Kwota" Width="60" IsReadOnly="True" Binding="{Binding Amount}"/>
                <DataGridTextColumn Header="Data" Width="75" IsReadOnly="True" Binding="{Binding Date}"/>
            </DataGrid.Columns>
        </DataGrid>
        <DataGrid HorizontalAlignment="Left" Name="providedPaymentsDataGrid" ItemsSource="{Binding}" AutoGenerateColumns="False" CanUserResizeColumns="False" Margin="10,38,0,0" VerticalAlignment="Top" Height="234" Width="375" Loaded="Page_Loaded" SelectionMode="Single" LoadingRow="ProvidedPaymentsDataGrid_OnLoadingRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="+/-" Width="40" IsReadOnly="True" Binding="{Binding Sign}"/>
                <DataGridTextColumn Header="Typ" Width="70" IsReadOnly="True" Binding="{Binding Type}"/>
                <DataGridTextColumn Header="Nazwa" Width="105" IsReadOnly="True" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Kwota" Width="60" IsReadOnly="True" Binding="{Binding Amount}"/>
                <DataGridTextColumn Header="Data" Width="75" IsReadOnly="True" Binding="{Binding Date}"/>
            </DataGrid.Columns>
        </DataGrid>
        <DataGrid HorizontalAlignment="Right" VerticalAlignment="Bottom" Height="160" Width="300" Margin="0,0,10,10"/>
        <Label Content="Najbliższe przychody i wydatki:" HorizontalAlignment="Left" Height="28" Margin="10,10,0,0" VerticalAlignment="Top" Width="191"/>
        <Label Content="Cele oszczędzania:" HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="300" Height="28" Margin="0,0,10,170"/>
        <Label Content="Skrócona historia:" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="164" Height="28" Margin="10,0,0,244"/>
        <Label Content="Aktualne saldo:" HorizontalAlignment="Right" VerticalAlignment="Top" Width="94" Height="25" Margin="0,10,75,0"/>
        <TextBox Name="Balance" HorizontalAlignment="Right" VerticalAlignment="Top" Width="65" Height="22" Margin="0,13,10,0" TextWrapping="Wrap" IsReadOnly="True" Text="TextBox"/>
        <ProgressBar x:Name="PaymentsBar" HorizontalAlignment="Left" Height="165" Margin="466,107,0,0" VerticalAlignment="Top" Width="33" Orientation="Vertical" Background="White" Foreground="#FFB41919"/>
        <ProgressBar x:Name="SalariesBar" HorizontalAlignment="Left" Height="165" Margin="539,107,0,0" VerticalAlignment="Top" Width="33" Orientation="Vertical">
            <ProgressBar.Foreground>
                <SolidColorBrush Color="#FF0C3A0C"/>
            </ProgressBar.Foreground>
            <ProgressBar.Background>
                <SolidColorBrush Color="White"/>
            </ProgressBar.Background>
            <ProgressBar.BorderBrush>
                <LinearGradientBrush EndPoint="0.5,1" MappingMode="RelativeToBoundingBox" StartPoint="0.5,0">
                    <GradientStop Color="#FFB2B2B2" Offset="0"/>
                    <GradientStop Color="#FF8C8C8C" Offset="1"/>
                </LinearGradientBrush>
            </ProgressBar.BorderBrush>
        </ProgressBar>
    </Grid>
</Page>
